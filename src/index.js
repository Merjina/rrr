import React from 'react';
import ReactDOM from 'react-dom/client';
import Stu from './component/com/Stu';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Stu />
  </React.StrictMode>
);